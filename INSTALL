Requirements
============

Needed to compile DbMaster :
  * Qt (>=4.5) with QtSql module
  * make
  * g++

Windows instructions comming soon.

Building
========

Open a terminal and run :

  qmake
  make

When it's finished, you can run DbMaster by typing :

  ./bin/dbmaster

Install
=======

Once DbMaster is compiled, just type (as root) the following command :

  make install
